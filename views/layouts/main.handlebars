<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ONLINE MIXTAPE GENERATOR</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Notable&family=Orbitron:wght@700;900&family=Press+Start+2P&family=Quantico:ital@0;1&family=Sarpanch:wght@400;600;900&family=Share+Tech+Mono&family=VT323&display=swap" rel="stylesheet">

</head>

<body>
  <div class="flex-column justify-space-around">
    <header>
      <h1>
        <a href="/" class="title">ONLINE JUKEBOX GENERATOR!</a>
      </h1>

      <div class="img-container">
        <img src="/jukeicon.png" alt="jukeicon.png">
      </div>

      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" class="log">profile</a> |
        <button class="no-button log" id="logout">logout</button>
        {{else}}
        <a href="/login" class="log">login</a>
        {{/if}}
      </nav>
    </header>


    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer>
      <p> SHURIKEN BEBE 2023</p>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="./js/login.js"></script>
  {{/if}}
</body>

</html>