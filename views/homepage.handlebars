<!-- Loop over each object in the songs array -->
{{#each songs as |song| }}
<div class="row mb-4 song">
  <div class="col-md-5">
    <h2></h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{!-- {{{get_emoji}}} --}}

      <a href="/song/{{song.id}}">{{song.name}}</a>
    </h2>

    <p>
      {{!-- <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed! --}}
    </p>
    {{!-- <p>Created by {{playlist.user.name}} on {{format_date playlist.date_created}}</p> --}}
  </div>
  {{!-- <div class="col-md-7">
    <p>
      {{}}
    </p>
  </div> --}}
</div>
{{/each}}
